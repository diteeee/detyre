clone the repository:

```
git clone https://github.com/diteeee/detyre.git
cd detyre
```

install dependencies:

```
npm install
```

build the container:

```
docker-compose up --build
```

the app should now be running on:

```
http://localhost:3000
```

to stop the app:

```
docker-compose down
```
